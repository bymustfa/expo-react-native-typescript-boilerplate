import React, { FC } from "react";
import { Text, ScrollView } from "components/base";

const HomeScreen: FC = () => {
  return (
    <ScrollView flex={1}>
      <Text>Home Screen</Text>
    </ScrollView>
  );
};

export default HomeScreen;
